{% extends '::base.html.twig' %}

{% block body -%}
    <h1>stations</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Stationname</th>
                <td>{{ entity.stationName }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('timetable_stations') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('timetable_stations_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
