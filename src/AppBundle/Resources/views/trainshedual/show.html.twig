{% extends '::base.html.twig' %}

{% block body -%}
    <h1>trainshedual</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Operatingdays</th>
                <td>{{ entity.operatingdays }}</td>
            </tr>
            <tr>
                <th>Schedule</th>
                <td>{{ entity.schedule }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('trainshedual') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('trainshedual_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
