{% extends '::base.html.twig' %}

{% block body -%}
    <h1>schedulestations</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Station</th>
                <td>{{ entity.stationId }}</td>
            </tr>
            <tr>
                <th>Arivaltime</th>
                <td>{{ entity.arivalTime|date('H:i')  }}</td>
            </tr>
            <tr>
                <th>Departuretime</th>
                <td>{{ entity.departureTime|date('H:i')  }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('timetable_schedulestations') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('timetable_schedulestations_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
