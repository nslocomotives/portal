{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Locos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Loconumber</th>
                <th>Loconame</th>
                <th>Operationalstatus</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('locos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.locoNumber }}</td>
                <td>{{ entity.locoName }}</td>
                <td>{{ entity.operationalStatus }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('locos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('locos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('locos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
